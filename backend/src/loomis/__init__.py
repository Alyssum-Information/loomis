"""Loomis backend — local-first voice lifelogger.

See ../../docs for the design. This package hosts the core, pipeline, daemon,
and FastAPI API. Pre-alpha: only the walking skeleton (config + DB + health API)
exists so far.
"""

__version__ = "0.0.0"
